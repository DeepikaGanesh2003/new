import { SampleDirectiveDirective } from './sample-directive.directive';

describe('SampleDirectiveDirective', () => {

  beforeEach(()=>{
    declaration: [SampleDirectiveDirective]
  })
  it('should create an instance', () => {
    // const directive = new SampleDirectiveDirective(fixture.debugElement);
    // expect(directive).toBeTruthy();
  });
});
